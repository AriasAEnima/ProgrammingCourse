# Homework

Create your topic App Django + Mongo DB composed.

## Requirements:

* API CRUD of your topic Items
* Have a rendered view (as /dynamic-pages example) WITHOUT auth
* Have a controlled endpoints on /api/ paths using JWT. 

## Expected 

I expect only run *docker-compose up --build*

and everything must work, (on /api/... must fail by auth)